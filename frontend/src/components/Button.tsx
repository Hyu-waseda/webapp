import { ReactElement } from "react";
import styles from "components/Button.module.scss";

export const Button = (): ReactElement<any, any> => {
  return (
    <button className={styles.button}>hgoe</button>
  );
};